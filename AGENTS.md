# my-site 项目说明

> 任何 AI（Cursor / Hermes / Codex）改这个页面时，**先读这份说明**，按下面的规矩来。
> 负责人：周万里。风格原则：简约明了，让人一眼抓到重点。

## 设计规矩

1. **风格**：极简、大量留白，不做多余装饰。
2. **焦点**：一屏只强调一个重点（姓名最大最醒目），信息层级清楚，别人一眼能抓到重点。
3. **配色**：
   - 浅色模式：浅蓝渐变，从左到右 `#d4ebf8 → #ffffff`。
   - 深色模式：夜晚暗紫渐变，从左到右 `#211a38 → #110d1e`。
4. **排版**：标题 44px / 字重 600；正文 16px、行距 2.0。
5. **动效**：背景粒子克制、不抢内容；深色模式下粒子切换为「萤火虫」（暖黄绿 + 光晕 + 呼吸式明灭）。
   粒子交互：电脑端——鼠标周围 300px 内的粒子会聚拢，点击时向外散开；手机端——只有轻点散开，不做聚拢。

## 技术约束

- 三个页面（`index.html` / `works.html` / `about.html`）共用一份 `style.css` 和一份 `particles.js`（背景粒子），不引外部依赖。
- 深色模式用 `body.dark` 类切换（JS 在 `themeToggle` 上挂点击事件）。
- 改动后务必在浏览器打开验证：浅色 / 深色都要看一遍。
